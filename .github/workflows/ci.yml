name: ci

permissions:
  contents: read

on:
  push:
    branches: [main, next-level, 'feat/**']
  pull_request:
  schedule:
    - cron: '17 2 * * 0'
  workflow_dispatch:

jobs:
  scope:
    runs-on: ubuntu-latest
    outputs:
      quick: ${{ steps.scope.outputs.quick }}
      typecheck: ${{ steps.scope.outputs.typecheck }}
      legacy: ${{ steps.scope.outputs.legacy }}
      curated: ${{ steps.scope.outputs.curated }}
      schema: ${{ steps.scope.outputs.schema }}
      site: ${{ steps.scope.outputs.site }}
      full: ${{ steps.scope.outputs.full }}
    steps:
      - uses: actions/checkout@v7
        with: { fetch-depth: 0 }
      - name: Select affected checks
        id: scope
        env:
          EVENT_NAME: ${{ github.event_name }}
          BEFORE_SHA: ${{ github.event.before }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          if [[ "$EVENT_NAME" == workflow_dispatch || "$EVENT_NAME" == schedule ]]; then
            CI_SCOPE_REASON="$EVENT_NAME" node tools/ci-scope.mjs --full
            exit 0
          fi
          BASE_SHA="$BEFORE_SHA"
          if [[ "$EVENT_NAME" == pull_request ]]; then BASE_SHA="$PR_BASE_SHA"; fi
          if [[ -z "$BASE_SHA" || "$BASE_SHA" =~ ^0+$ ]] || ! git cat-file -e "${BASE_SHA}^{commit}"; then
            CI_SCOPE_REASON="missing base commit" node tools/ci-scope.mjs --full
            exit 0
          fi
          git diff --name-only --no-renames --diff-filter=ACMRTD "$BASE_SHA" "$HEAD_SHA" | node tools/ci-scope.mjs

  quick-checks:
    needs: scope
    if: needs.scope.outputs.quick == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with: { node-version-file: '.nvmrc', cache: npm }
      - run: npm ci
      - run: npm run test:unit
      - if: needs.scope.outputs.typecheck == 'true'
        run: npm run typecheck
      - name: index.html matches src/
        if: needs.scope.outputs.legacy == 'true'
        run: |
          cp index.html /tmp/committed.html
          npm run build
          cmp -s /tmp/committed.html index.html || {
            echo "::error::index.html is stale — run 'npm run build' and commit it"
            exit 1
          }

  legacy-browser:
    needs: scope
    if: needs.scope.outputs.legacy == 'true'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        shard: [1, 2, 3, 4, 5, 6]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with: { node-version-file: '.nvmrc', cache: npm }
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run archive
      - run: npx playwright test --shard=${{ matrix.shard }}/6
      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: legacy-playwright-report-${{ matrix.shard }}
          path: test-results/
          retention-days: 7

  curated-browser:
    needs: scope
    if: needs.scope.outputs.curated == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with: { node-version-file: '.nvmrc', cache: npm }
      - run: npm ci
      - run: npx playwright install --with-deps chromium
      - run: npm run test:curated
      - uses: actions/upload-artifact@v7
        if: failure()
        with: { name: curated-playwright-report, path: test-results/, retention-days: 7 }

  schema:
    needs: scope
    if: needs.scope.outputs.schema == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # supabase-setup.sql once failed to parse and applied nothing at all,
      # silently, leaving a gallery that was world-readable while its owner
      # believed otherwise. Reading the file is not evidence; executing it is.
      - name: row-level security actually applies
        run: npm run verify:sql

  required:
    if: ${{ always() }}
    needs: [scope, quick-checks, legacy-browser, curated-browser, schema]
    runs-on: ubuntu-latest
    steps:
      - name: Require every selected check
        env:
          SCOPE: ${{ needs.scope.result }}
          QUICK: ${{ needs.quick-checks.result }}
          LEGACY: ${{ needs.legacy-browser.result }}
          CURATED: ${{ needs.curated-browser.result }}
          SCHEMA: ${{ needs.schema.result }}
        run: |
          for result in "$SCOPE" "$QUICK" "$LEGACY" "$CURATED" "$SCHEMA"; do
            case "$result" in success|skipped) ;; *) exit 1 ;; esac
          done

  pages:
    # `always()` prevents intentionally skipped, unrelated suites from
    # propagating a hidden skip through the required aggregate.
    if: ${{ always() && github.event_name == 'push' && github.ref == 'refs/heads/main' && needs.scope.outputs.site == 'true' && needs.required.result == 'success' }}
    needs: [scope, required]
    runs-on: ubuntu-latest
    concurrency:
      group: github-pages
      cancel-in-progress: false
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with: { node-version-file: '.nvmrc', cache: npm }
      - run: npm ci
      - run: npm run build:site
      - uses: actions/upload-artifact@v7
        with: { name: gallery-site, path: site/, retention-days: 7 }
      # upload-pages-artifact@v4 still embeds a Node 20 action. This is the
      # same archive format, uploaded by the Node 24-native artifact action.
      - name: Archive Pages artifact
        run: |
          tar \
            --dereference --hard-dereference \
            --directory site \
            -cvf "$RUNNER_TEMP/artifact.tar" \
            --exclude=.git \
            --exclude=.github \
            --exclude=".[^/]*" \
            .
      - uses: actions/upload-artifact@v7
        with:
          name: github-pages
          path: ${{ runner.temp }}/artifact.tar
          retention-days: 1
          if-no-files-found: error
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
